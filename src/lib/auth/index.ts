/**
 * Auth module barrel export
 * ────────────────────────────────────────────────────────────────────────────
 * Server-side:  import { auth } from "@/lib/auth";
 * Client-side:  import { signIn, useSession } from "@/lib/auth/client";
 */

export { auth } from "./server";
